import { OverlayAttachDirective } from './overlay-attach.directive';

describe('OverlayAttachDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayAttachDirective();
    expect(directive).toBeTruthy();
  });
});
